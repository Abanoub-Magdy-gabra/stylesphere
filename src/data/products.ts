// This file is now deprecated in favor of Supabase data
// Keeping it for backward compatibility during migration

import { Product } from '../types/product';

export const featuredProducts: Product[] = [
  // This data is now stored in Supabase
  // Use the useProducts hook to fetch products instead
];